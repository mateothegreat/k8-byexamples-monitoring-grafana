apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-persistent-storage
  labels:
    app: grafana-persistent-storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
